# Instructions

The master instructions file is at the vault root: `instructions.md`.

This file exists only as a pointer. For the full operating guide, read `instructions.md` at the root of the wiki vault.
